<import name='quist-search' src='@quist-ui/quist-search/index'></import>

<template>
  <div class="input-demo">
    <div class="item-box">
      <text class="item-txt">默认输入框</text>
      <quist-search placeholder='请输入关键字' autofocus vlaue="{{value1}}" @on-change="onChange1"></quist-search>
    </div>
    <div class="item-box">
      <text class="item-txt">禁用状态的输入框</text>
      <quist-search placeholder='请输入关键字' disabled></quist-search>
    </div>
    <div class="item-box">
      <text class="item-txt">带前缀的输入框</text>
      <quist-search placeholder='请输入关键字' prefix="{{prefix}}" value="{{value2}}" @on-change="onChange2" suffix="{{suffix}}" wrapper-style="{{wrapperStyle}}"></quist-search>
    </div>
    <div class="item-box">
      <text class="item-txt">带前缀与后缀的输入框</text>
      <quist-search placeholder='请输入关键字' prefix="{{prefix}}" value="{{value3}}" @on-change="onChange3" suffix="{{suffix}}" wrapper-style="{{wrapperStyle}}"></quist-search>
    </div>
    <div class="item-box">
      <text class="item-txt">自定义样式的输入框</text>
      <quist-search placeholder='请输入关键字' prefix="{{prefix}}" value="{{value4}}" @on-change="onChange4" suffix="{{suffix}}" wrapper-style="{{wrapperStyle1}}" input-style="{{inputStyle}}"></quist-search>
    </div>
    <div>
</template>

<style>
  .input-demo {
    flex: 1;
    flex-direction: column;
    background-color: #ffffff;
  }

  .item-box {
    margin: 20px;
    padding-bottom: 50px;
    flex-direction: column;
  }

  .item-txt {
    width: 100%;
    margin-bottom: 30px;
    font-size: 32px;
    color: #666666;
  }
</style>

<script>
  import prompt from '@system.prompt'

  export default {
    data: {
      prefix: 'search.png',
      suffix: 'close.png',
      value1: '',
      value2: '',
      value3: 'hello world',
      value4: '你好, 世界',
      wrapperStyle: {
        height: '68px',
        width: '560px',
        borderRadius: '50px',
        border: '10px solid #E3E5E9'
      },
      iconStyle: {

      },
      wrapperStyle1: {
        height: '68px',
        width: '560px',
        borderRadius: '50px',
        border: '10px solid #E3E5E9',
        backgroundColor: '#ffffff'
      },
      inputStyle: {
        color: '#1890ff'
      }
    },
    onInit() {
      this.$page.setTitleBar({ text: 'Search' })
    },
    search(e) {
      prompt.showToast({
        message: 'value:' + e.detail.value
      })
    },
    onChange1(e) {
      this.setValue(e, 1)
    },
    onChange2(e) {
      this.setValue(e, 2)
    },
    onChange3(e) {
      this.setValue(e, 3)
    },
    onChange4(e) {
      this.setValue(e, 4)
    },
    setValue(e, index) {
      let value = e.detail.value;
      this[`value${index}`] = value;
      prompt.showToast({
        message: `value${index}: ` + value
      })
    }
  }
</script>